[project]
name = "django-strava"
version = "0.1.0"
description = "Django app to extract and display Strava activities."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2.7",
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
]
